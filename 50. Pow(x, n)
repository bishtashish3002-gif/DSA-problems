class Solution {
    public double myPow(double x, int n) {
        double result=0;
        if(n>0){
            result = Math.pow(x,n);
        }else{
            result = 1/(Math.pow(1/x,n));
        }
        return result;
    }
}
