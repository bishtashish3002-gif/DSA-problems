class Solution {
    public int[] sortedSquares(int[] nums) {
        int [] newarr = new int[nums.length];
        int i = 0;
        for(int num : nums){
                newarr[i] = (num*num);
                i++;
            }
        Arrays.sort(newarr);
        return newarr;
    }
}
