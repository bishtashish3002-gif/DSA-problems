// code in java 


class Solution {
    public int arrayPairSum(int[] nums) {
        Arrays.sort(nums);
        int n=nums.length, s= 0;
        for(int i=0; i<n; i+=2){
            s += nums[i];
        }
        return s;
    }

}
