class Solution {
    public int maximumGap(int[] nums) {
        int MaxGap =0, gap=0;
        Arrays.sort(nums);
        for (int i =0; i<nums.length-1; i++){
            gap = nums[i+1] - nums[i];
            MaxGap =Math.max(MaxGap,gap);
        }
        return MaxGap;
    }
}
